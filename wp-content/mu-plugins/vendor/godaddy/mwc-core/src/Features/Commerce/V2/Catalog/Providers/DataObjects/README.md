# Provider Data Objects

These data objects match schemas defined by the Commerce Providers. They are used to interact with the provider APIs. We (MWC) do not control the schema ourselves; we simply match what the API has already defined.
